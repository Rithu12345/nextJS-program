'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button onClick={()=> console.log('Click')}>Add to the Cart</button>
    </div>
  )
}

export default AddToCart
